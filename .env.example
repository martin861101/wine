# Runtime configuration belongs to each deployable application:
#   ui/.env.example      -> ui/.env
#   backend/.env.example -> backend/.env
#
# The root .env file is retained only for compatibility with older checkouts.
