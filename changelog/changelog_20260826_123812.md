# Backend change log — 2026-08-26 12:38 SAST

## Implemented

- Added a safe Supabase migration for every structured book-review form value and legacy fallbacks.
- Updated review submission, public publication, admin moderation, TypeScript mappings, and review RLS.
- Removed custom approval as an access requirement; verified registrations now receive normal member access automatically.
- Added explicit blocked/removed status while retaining the legacy `approved` compatibility field.
- Added the protected `admin-members` Edge Function for reset, confirmation resend, block, unblock, and deliberate removal operations.
- Added self-protection, last-active-admin protection, server-side trusted role checks, UUID/action validation, and success/failure audit records.
- Preserved community content on removal by anonymising the application/profile row instead of cascading deletion.
- Added member list status fields and frontend API contracts.
- Added an RLS-protected admin fallback switch that redirects public routes to `/fallback.html` while preserving admin/Auth access.
- Added `ui/fallback.html` as an explicit Vite entry so production builds include web-root `dist/fallback.html`.
- Updated registration/access copy, backend documentation, and README guidance.

## Verification

- Full UI/backend TypeScript typecheck passed.
- All six backend test files passed, including structured-review/member-admin contract checks.
- Full UI/backend lint passed with seven existing non-failing Fast Refresh warnings.
- Production UI build passed.
- The entire migration chain applied cleanly to a disposable local Supabase stack; local and linked database lint returned no schema errors.
- Local integration checks verified automatic `MEMBER` registration, standard-member `403`, password reset, confirmation resend, block/login denial, unblock/login restoration, self-protection, serialized last-admin protection, deliberate removal/anonymisation, audit writes, and every structured review field through publication.
- Production migration `20260826120000` was applied to project `ykyzelgoeblxhcdguyww`.
- Production Edge Functions deployed and verified active: `admin-members` v1, `broadcast` v20, `ai-chat` v17, and `open-library-search` v3.
- The deployed `admin-members` endpoint returned `401 AUTH_REQUIRED` without a caller JWT.
- The frontend build passed with `dist/fallback.html` present, but was not uploaded to xneelo.
