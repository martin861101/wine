# Production administrator and backend audit

## Deployment

- Created and email-confirmed the production Supabase Auth account for `hello@wineandchapters.co.za`.
- Linked and promoted the application user to verified, approved `ADMIN` access.
- Confirmed password authentication, the administrator overview RPC, member home RPC, and authenticated AI chat all return HTTP 200.
- Confirmed all database migrations are synchronized, database lint is clean, required PostgREST RPCs are present, protected tables enforce RLS, and the `event-photos` Storage bucket is active.

## SMTP resolution

- The subsequent SMTP retest authenticated successfully and xneelo accepted the test message.
- The deployed Supabase contact function returned HTTP 200 and recorded its SMTP message as delivered.
- Supabase Auth accepted an administrator password-recovery email request with HTTP 200.
- The current `backend/.env`, `.env.supabase`, and deployed Edge Function SMTP password values match.

The generated administrator password is intentionally not stored in the repository.
