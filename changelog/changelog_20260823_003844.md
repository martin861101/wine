# Homepage hero redesign — 2026-08-23 00:38:44

## Changed

- Replaced the homepage video/card hero presentation with the supplied editorial artwork.
- Added responsive source selection for `hero_bg.png` and the confirmed mobile asset `hero_mobile.png` at the project's 768px breakpoint.
- Added an organic artwork reveal, staged copy entrance, slow image drift, restrained desktop pointer depth, scroll transition, and lightweight petals.
- Added a complete reduced-motion state that removes reveal, parallax, drift, and decorative movement.
- Updated the hero hierarchy and connected CTAs to the existing registration route and dynamic monthly-read section.
- Preserved the existing navigation, About storybook, authentication, and all later homepage sections.

## Validation

- `npm run build` — passed.
- `npm run lint` — hero code passed; the command remains blocked by pre-existing stray `codex` text in `ui/src/hooks/use-mobile.tsx`.
- `npm run typecheck` — hero code passed; the command remains blocked by pre-existing errors in `ui/src/hooks/use-mobile.tsx` and possible-undefined page access in `ui/src/components/story-book.tsx`.
- Visually checked 375px, 430px, tablet portrait, 1366px, 1920px, and reduced-motion compositions.
