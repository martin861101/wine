# Changelog — 2026-08-19 12:05:45 SAST

## Two-sided About books with adjacent paper-framed video

- Reworked the homepage About block and full `/about` reader so each book has two equal sides:
  the active story page on the left and the supplied second-logo cover on the right.
- Moved the About clips out of the book pages into separate adjacent panels with translucent noise,
  crease and paper-wash treatments.
- Added keyed video remounts so page turns load the changed numbered MP4 files instead of retaining
  the previous clip in the browser.
- Kept the slower page-turn animations, enlarged AI reading-room launcher, and non-profit legal
  footer pages in the updated documentation.

## Verification

- `npm --prefix ui run typecheck`
- `npm --prefix ui run lint` (existing Fast Refresh warnings only)
- `npm --prefix ui run build`
- Captured and inspected desktop and mobile screenshots for the homepage About block and full
  `/about` reader, including a second-page turn with `/videos/2.mp4` confirmed as the active source.
