# Changelog — 2026-08-17 11:58:36

## Added local deployment and testing workflow

- Added a Docker Compose PostgreSQL service matching the development `DATABASE_URL`.
- Added scripts for local database lifecycle, setup, application startup, verification, and Firebase emulators.
- Made local development/build commands force `NODE_ENV=development` and aligned local URL defaults with port 5178.
- Configured Firebase Functions, Hosting, and Emulator UI ports.
- Assigned the Emulator UI port 4401 to avoid the existing local service on port 4000 and Firebase's emulator hub port 4400.
- Documented the SSR application workflow and the distinction between app testing and static Firebase Hosting emulation in `README.md`.
