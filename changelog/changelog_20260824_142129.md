# Changelog — 2026-08-24 14:21:29

## Changed

- Reordered the homepage so Current Book and the existing review interaction appear early, while member perspectives form the final content section.
- Replaced the homepage's single-event preview with a responsive, accessible calendar backed by the existing public events endpoint.
- Added reusable, responsive watercolor edge decoration using the supplied transparent asset.
- Removed the large tinted background from the homepage benefits area and retained gradients only as restrained card/edge treatments.
- Added correctly mapped public reviewer profile images with branded initials as the fallback.
- Renamed Membership to Shop in shared navigation and footer links, with a dedicated `/shop` destination.
- Added a minimal Shop coming-soon experience and updated its metadata; `/membership` redirects to the intact `/register` membership application flow for backward compatibility.
- Kept the public contact email at `hello@wineandchapters.co.za` and did not change transactional email configuration.
- Fixed existing StoryBook empty-page narrowing and a stray mobile-hook token so frontend validation can complete.

## Backend

- Added `20260824120000_public_review_profile_images.sql` to return only the matching review author's public avatar URL from `get_published_reviews()`.

## Validation

- `npm --prefix ui run typecheck`
- `npm --prefix ui run lint`
- `npm run build`
- Responsive browser checks at 375, 768, 1024, and 1440 pixels; no horizontal overflow detected.
- Browser assertions confirmed multi-event rendering, event-date accessible labels, Shop metadata, Shop navigation labels, and the public contact email.
