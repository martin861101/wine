# Administrator workflows, event images, and Paystack

## Fixed

- Replaced the partial external-book index with a PostgREST-compatible unique constraint, fixing manual book import and current-read selection.
- Added validation to current-read dates and book selection.
- Added an administrator event-image control with file-type and 8 MB size validation.
- Connected event images to Supabase Storage, `event_photos`, and `events.cover_image`.
- Added the authenticated `get_events()` RPC and changed the member Events page to render live Supabase events and uploaded covers instead of sample data.
- Retained the public sample event view for signed-out visitors.

## Paystack

- Added server-defined Chapter Member and Patron checkout amounts.
- Added `membership_orders` and membership status fields.
- Added and deployed `paystack-checkout` for initialization and return verification.
- Added and deployed `paystack-webhook` with HMAC-SHA512 signature verification and idempotent event storage.
- Connected paid membership buttons to hosted Paystack checkout without exposing the secret key.

## Verification

- Applied migration `20260817213019_admin_workflow_fixes.sql`; remote database lint is clean.
- Passed real authenticated tests for book import, current read, progress, event creation, image upload/public read, live event retrieval, announcements, ratings, RSVP, polls, discussions, newsletter, member administration, AI, and SMTP broadcast.
- TypeScript, static production build, and ESLint completed with no errors.
- All five Supabase Edge Functions report `ACTIVE`.

## Remaining configuration

- Paystack rejected the configured value with HTTP 401 `Invalid key`. The value in `backend/.env` and the deployed Supabase secret match, but it is not a valid `sk_test_...` or `sk_live_...` secret key.
- Replace `PAYSTACK_SECRET_KEY`, push the updated Supabase secret, and rerun an unpaid checkout initialization before accepting payments.

All disposable database and Storage audit records were removed after testing.
