# Supabase-native backend and xneelo UI

## Changed

- Replaced the UI's dependency on the standalone Node REST API with the official Supabase browser client.
- Added Supabase Auth signup, verification, login, session refresh, logout, and password recovery flows.
- Configured `wineandchapters.co.za` as the Supabase Auth site URL and trusted verification/reset origin.
- Added the complete Supabase schema, Auth-to-member linkage, row-level security, browser-safe RPC functions, and Storage policies.
- Added and deployed `contact` and administrator-only `broadcast` Edge Functions with production-origin CORS enforcement.
- Configured the xneelo UI with the project's public Supabase URL and publishable key.
- Removed the local `/api` and `/uploads` Vite proxy and the standalone backend PM2 process.
- Protected `.env.supabase` and Supabase CLI temporary state from Git.
- Converted the xneelo frontend from a Node/SSR output to a static Vite/TanStack Router SPA.
- Added a generated `ui/dist/index.html` entry point and Apache `.htaccess` history fallback for direct client-side routes.

## Deployment

- Applied remote migration `20260817145733_supabase_native_backend.sql`.
- Added a safe Auth-account backfill that preserves existing application roles and leaves new accounts pending explicit approval.
- Applied the production Supabase Auth URL, callback, password, confirmation, and xneelo SMTP configuration.
- Confirmed the remote database lint has no schema errors and both Edge Functions are active.

## Remaining configuration

- Explicitly choose the first confirmed administrator account; no email address is automatically granted administrator access.
- Install/activate an xneelo TLS certificate covering `wineandchapters.co.za`; the host currently presents only `*.jnb3.host-h.net`.
- Replace the current xneelo SMTP password: direct authentication returned `535 Login denied` during deployment verification.

## SMTP update

- Replaced the Edge Functions' Resend transport with the existing xneelo SMTP mailbox.
- Contact delivery, Supabase Auth email, and administrator broadcasts now share the configured SMTP service.
- Switched SMTP TLS connections to xneelo's certificate-valid `www7.jnb3.host-h.net` hostname.
