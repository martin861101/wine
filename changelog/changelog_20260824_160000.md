# Changelog — 2026-08-24 16:00:00

## Fixed

- Removed the unsupported `additionalProperties` keyword from all 10 Gemini function declarations used by `ai-chat`.
- Retained the existing server-side argument type, length, enum, and allowlist validation for every tool execution path.
- Preserved the Gemini error-body diagnostics added previously.

## Validation

- Backend typecheck passed.
- Backend test suite passed: 3 test files, 0 failures.
- Audit confirmed all 10 model-facing declarations contain no `additionalProperties` keyword.
- Targeted `ai-chat` deployment succeeded; remote status is `ACTIVE` at version 12.
- Live `Hello` smoke test returned a normal Gemini response with no HTTP 400.
- Live `Show me the events` smoke test returned a normal response and executed a `NAVIGATE` action.
