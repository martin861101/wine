# Live AI and Supabase connection fix

## Fixed

- Replaced the reading-room widget's hard-coded mock responses with calls to the Supabase `ai-chat` Edge Function.
- Added authenticated member validation and server-side Gemini requests to keep the API key out of the browser.
- Added current-club-book context and bounded chat history to AI prompts.
- Corrected migrated-account lookups to use `users.auth_user_id` and `app_user_id()` instead of assuming Supabase Auth IDs equal application user IDs.
- Added live connection, loading, authentication, and error states to the chat UI.

## Deployment

- Pushed the configured Gemini secret and model to the linked Supabase project.
- Deployed the `ai-chat` Edge Function.
- Rebuilt the static xneelo UI successfully.
- Confirmed the deployed Supabase endpoint is reachable and the configured Gemini model returns a successful response.
