# Production Supabase UI Build Configuration

Date: 2026-08-17

## Changes

- Added an ignored `ui/.env.production` populated with only browser-safe Supabase build variables.
- Added `ui/.env.production` to `.gitignore`.
- Restricted `.env.supabase` and `ui/.env.production` to owner-only permissions.
- Updated production build documentation.
- Generated and verified the Vite production output in `ui/dist/`.

## Verification

- `npm run build` completed successfully.
- The compiled UI contains the configured Supabase project URL.
- The compiled output includes `.htaccess`.
- No server-only secret identifiers were found in generated HTML, CSS, or JavaScript.
- No application endpoint in the production bundle targets localhost.
