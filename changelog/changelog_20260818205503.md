# Homepage hero shadow edge fix

Date: 2026-08-18 20:55:03 SAST

## Fixed

- Restored the animated botanical shadow and foreground table layers in the homepage hero.
- Moved the shadow raster beyond the hero bounds so its left image boundary cannot drift on-screen.
- Added a soft alpha mask and additional animation scale to feather the shadow naturally at the
  edge while preserving the existing movement.
- Kept the reduced-motion fallback intact for visitors who disable animation.

## Verification

- `npm --prefix ui run typecheck`
- `npm --prefix ui run lint` (passes with 7 existing Fast Refresh warnings)
- `npm --prefix ui run build`
- Desktop homepage screenshots captured at multiple animation timings with no visible left seam.
