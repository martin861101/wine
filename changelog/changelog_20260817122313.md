# UI/backend split and Supabase integration — 2026-08-17 12:23 SAST

## Changed

- Split the browser application into `ui/` and the REST API/domain implementation into `backend/` npm workspaces.
- Configured the UI development server to proxy `/api` and `/uploads` to the backend on port `4010`, avoiding an existing service on port `4000`.
- Added separate PM2 processes for the UI and backend while retaining the public HTTPS hostname through path-based reverse proxying.
- Added Supabase Postgres configuration through `SUPABASE_DB_URL`/`DATABASE_URL`.
- Added a Supabase Storage provider using server-only service-role authentication.
- Added migration `006_supabase_storage.sql` for the default public photo bucket.
- Added `/api/health` for database readiness checks.
- Added per-workspace environment templates and protected backend secrets from accidental commits.
- Updated repository and backend documentation for local, PM2, HTTPS, and Supabase deployment.

## Validation

- UI TypeScript check passed.
- Backend TypeScript check passed.
- UI production build passed.
- Backend production build passed.
- UI and backend lint passed with no errors (seven existing Fast Refresh warnings remain in shared UI component modules).
- Backend unit tests passed.
- Firebase Functions lint and TypeScript build passed.
- Local migration `006_supabase_storage.sql` applied successfully to ordinary PostgreSQL as a no-op for Supabase-only objects.
- UI proxy integration returned `200` from `/api/health` with database connectivity confirmed.
