# Changelog — 2026-08-21 02:01:40 SAST

## Fixed

- Diagnosed the production Apache 403 as a missing `/public_html/index.html`; the remote directory contained only `client/` and `server/` from an incorrect build upload.
- Made `ui/ftp_sync` independent of the caller's working directory so it always builds and uploads `ui/dist/`.
- Added command, credential, and build-artifact preflight checks before the destructive FTP mirror begins.
- Added remote verification for `index.html` and `.htaccess` after upload.
- Added the root `npm run deploy:frontend` command.

## Documentation

- Added `docs/FRONTEND_DEPLOYMENT.md` with safe deployment, verification, and Apache 403 recovery instructions.
- Updated `README.md` with the guarded xneelo deployment workflow.
