# Changelog — 2026-08-24 18:16:58 SAST

## Homepage chapter rhythm and Events calendar

- Added subtle full-width homepage chapter surfaces while preserving the existing hero, About book, Current Read, Why Join, Join CTA and Newsletter designs.
- Added a compact Review and Gallery discovery strip between About and Current Read, plus a dedicated `/gallery` route using existing Wine & Chapters media.
- Removed the full review form and published book-review showcase from Home; `/reviews` remains the complete review experience.
- Restored club-focused member testimonials as a horizontal, swipe-capable carousel with labeled controls, reduced-motion support, Nici's supplied portrait and initials fallbacks for unmatched people.
- Replaced Home's month calendar with the next dynamic upcoming-event teaser and a graceful empty/error state.
- Added a shared real month calendar to `/events` with month navigation, accessible event markers, initial relevant-month selection, date selection and same-day event switching.
- Reused `MemberEventCard` inside calendar details, preserving authenticated RSVP behavior, seat counts, contribution amounts and the existing Paystack contribution card.
- Reduced the AI launcher footprint on small screens only to avoid obscuring calendar controls; AI behavior and integration code remain unchanged.
- Verified UI and backend typechecks, production build, lint with existing Fast Refresh warnings only, live desktop/mobile rendering, real Calendar-tab pointer interaction, same-day event selection and zero horizontal overflow at 1440px and 375px.
