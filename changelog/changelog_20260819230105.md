# Changelog — 2026-08-19 23:01:05 SAST

## Fixed

- Diagnosed the payment failure as a stale production `paystack-checkout` Edge Function;
  it interpreted event contributions as paid-membership requests.
- Deployed current `paystack-checkout` and `paystack-webhook` functions with contribution
  support; both are active at version 3.
- Verified the contribution action reaches its own validation path without creating a
  checkout or charge.
- Updated the UI API layer to extract Edge Function JSON error messages instead of showing
  Supabase's generic non-2xx message.

## Verification

- The supplied Paystack test credential successfully authenticated against Paystack.
- Production contribution validation returned the expected HTTP 400 response and message.
- UI typecheck and production build pass.
